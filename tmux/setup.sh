#! /bin/bash
cp ./.tmux.conf ~/.tmux.conf
echo "To install the plugin manager:"
echo "Step 1) git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm"
echo "Step 2) Reload tmux if it's already started with leader + r "
echo "Step 3) Launch tmux and hit leader + I (capital i) to fetch any plugins"
